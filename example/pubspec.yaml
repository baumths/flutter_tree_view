name: example
description: An example for the flutter_fancy_tree_view package.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  flutter_fancy_tree_view:
    path: ../

  flutter_speed_dial: ^3.0.5
  url_launcher: ^6.0.4


dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.0

flutter:
  uses-material-design: true