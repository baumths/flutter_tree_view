name: example
description: Example for the flutter_fancy_tree_view package.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.0.0

dependencies:
  faker: ^2.1.0
  flutter:
    sdk: flutter
  flutter_fancy_tree_view:
    path: ../
  path_drawing: ^1.0.1
  provider: ^6.0.5

dev_dependencies:
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
